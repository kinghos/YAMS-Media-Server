{
  "category": "General",
  "changelog": "Moonbase 2.2.0 adds server messages, so an admin can write something once and have it show up in people\u0027s apps even when they were offline while it was sent. The admin defaults page grows a lot, with settings for another 96 options and fixes to the six controls that were writing values no client reads. Retro games find more of their artwork, and it now picks up new ROMs without a restart. A reinstalled Seerr no longer strands you logged out, and a zipped game now arrives as the ROM it actually is. The bundled web client moves up to Moonfin 2.5.1.\n\nFeatures:\n\n- A new Messages tab lets an admin write a message with a title and a Markdown body, pick a colour, choose how it shows up, pick who sees it, schedule a start and end date, add a link button, and order the list. Broadcast now saves what it sends, so a user who was offline still finds it.\n- Admin defaults picked up settings for another 96 options, covering discovery rows, Since You Watched, music rows, sort direction for the home rows, player buttons, media segments, Cinema Mode for episodes, and a lot more.\n- Six admin controls that were writing values no client reads now send the right ones, and row sorting was rebuilt to match the clients, which also adds the missing Playlist Order option.\n- Retro games now watch the configured game library folders for file changes, so a new ROM or a per-library scan runs artwork reconciliation in the background instead of waiting for a restart, and games that came out as a false miss are retried once the matching has corrected them.\n- The settings sync toggle was renamed to Moonbase Sync, and its description now says that enabling it is required for the Seerr and ratings integrations.\n- The bundled web client picks up everything web relevant from Moonfin 2.5.1, including the messages window, a Seerr poster grid with a download progress bar, remote subtitle badges, Play and Resume on the long press menu, and personal ratings writing to Emby.\n\nBug Fixes:\n\n- A reinstalled Seerr no longer strands you logged out. The plugin was reading every 403 as missing admin rights, so the stale session was never cleared. Now it checks whether Seerr still knows the session first, and a definite refusal clears the old session and signs in again.\n- A zipped game now arrives as the ROM it actually is. The detail was naming the archive, so a client saved plain ROM bytes under a .zip name and failed to unpack them. It now names the file the endpoint actually sends, along with its real size.\n- A pending artwork timer can no longer take the process down on shutdown, and the artwork delivery route rejects an unknown role instead of falling through.\n- The websocket reconnects after a server restart, a busy server no longer reads as a connection timeout, and Escape closes the dialog on top instead of the page behind it.\n",
  "description": "Moonbase is the Moonfin server plugin providing shared infrastructure for every Moonfin client (web, mobile, desktop, and TV): cross-device settings sync with per-device profiles and admin defaults, hosting for the Moonfin Web app at /Moonfin/Web/, a theme editor with custom theme APIs, ratings integrations (MDBList and TMDB), and Seerr proxy with single sign-on. It also powers retro game libraries via EmulatorJS, with keyless libretro box art plus server-hosted cores and save sync.",
  "guid": "8c5d0e91-4f2a-4b6d-9e3f-1a7c8d9e0f2b",
  "name": "Moonbase",
  "overview": "Custom UI and settings sync for Jellyfin",
  "owner": "RadicalMuffinMan",
  "targetAbi": "10.10.0.0",
  "timestamp": "2026-08-28T22:51:05.0000000Z",
  "version": "2.2.0.0",
  "status": "Active",
  "autoUpdate": true,
  "imagePath": "/config/data/plugins/Moonbase_2.2.0.0/logo.png",
  "assemblies": [
    "Moonfin.Server.dll"
  ]
}